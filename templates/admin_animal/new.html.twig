{% extends 'base.html.twig' %}

{% block title %}New Animal{% endblock %}

{% block body %}
    <div class="container mx-auto p-4">
        <h1 class="text-2xl font-bold mb-4">Créer un nouvel animal</h1>

        <div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
            {{ include('admin_animal/_form.html.twig') }}
        </div>

        <a href="{{ path('app_admin_animal_index') }}" class="inline-block align-baseline font-bold text-sm text-blue-500 hover:text-blue-800">
            Retour à la liste
        </a>
    </div>
{% endblock %}
