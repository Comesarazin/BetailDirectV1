{% block nav %}
<nav class="p-4" style="background-color: #DBDCD7;">
    <div class="container mx-auto flex justify-between items-center">
        <div class="logo">
            <a href="{{ path('app_animal_index') }}">
                <img src="{{ asset('images/livestock_sales_logo.png') }}" alt="logo" class="h-20 w-20 rounded-full">
            </a>
        </div>
        <div class="menu">
            <ul class="flex space-x-4">
                <li><a href="{{ path('app_animal_index') }}" class="text-[#495C4A] hover:text-gray-400">Accueil</a></li>
                <li><a href="{{ path('contact') }}" class="text-[#495C4A] hover:text-gray-400">Contact</a></li>
                <li><a href="{{ path('about') }}" class="text-[#495C4A] hover:text-gray-400">A propos</a></li>
            </ul>
        </div>
    </div>
</nav>
{% endblock %}